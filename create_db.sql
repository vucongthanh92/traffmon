CREATE DATABASE traffic


                   (temp=20)      (host=R1)
     (CPU_LOAD)    (key-value)    (key-value)
     (       "unit"            ) ("metadata")          
time, measurement,  field,           tags


Interfaces can be TABLE
AS can be table

Measurement = TABLE
tags = COLUMNS     INDEXED
filed = COLUMNS
